<% title "Pages" %>

<p><%= link_to 'New Page', new_page_path %></p>

<% @pages.each do |page| %>
  <div>
	  <h2><%= link_to page.title, page_path(page) %></h2>
		<p><%= truncated_text page.content %> <%= link_to "Read more", page_path(page) %></p>
  </div>
<% end %>

<%= paginate @pages %> 